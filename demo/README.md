# Figma Home Flutter Web Demo

This demo is the runnable artifact used in the talk.

## Run

```bash
flutter pub get
flutter test
flutter run -d chrome --web-port 4174
```

The page renders a centered `390 x 844` mobile canvas and uses local assets from `assets/figma/`.
